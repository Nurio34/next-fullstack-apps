import React from "react";
import { Task as TaskType } from "@prisma/client";

function DeleteModal({ task, index }: { task: TaskType; index: number }) {
    return <div>DeleteModal</div>;
}

export default DeleteModal;
